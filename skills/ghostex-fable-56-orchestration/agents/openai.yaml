interface:
  display_name: 'ghostex-fable-56-orchestration'
  short_description: 'Fable plans, Codex gpt-5.6 workers implement, Fable verifies'
  default_prompt: 'Use $ghostex-fable-56-orchestration to plan a multi-phase task inline, launch a Codex gpt-5.6 worker pane per phase, then verify with a Fable pane and spawn fixers until verification passes.'

policy:
  allow_implicit_invocation: false
