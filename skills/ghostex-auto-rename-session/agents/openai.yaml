interface:
  display_name: 'Ghostex Auto Rename Session'
  short_description: 'Generate and submit a Ghostex session title'
  default_prompt: 'Use $ghostex-auto-rename-session to create a title under 60 characters and submit `/rename <title>` in the current Ghostex session.'

policy:
  allow_implicit_invocation: true
