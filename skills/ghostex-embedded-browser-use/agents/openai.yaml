interface:
  display_name: 'Ghostex Embedded Browser Use'
  short_description: 'Control Ghostex embedded browser panes via CLI'
  default_prompt: 'Use $ghostex-embedded-browser-use to control a Ghostex embedded browser pane through the Ghostex CLI entrypoint.'

policy:
  allow_implicit_invocation: true
