interface:
  display_name: 'Ghostex Browser Use'
  short_description: 'Automate browser pages through the Cua Driver CLI'
  default_prompt: 'Use $ghostex-browser-use to inspect and operate a supported browser page through the Cua Driver CLI, not MCP.'

policy:
  allow_implicit_invocation: false
