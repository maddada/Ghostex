node_modules/
build/
!.dependencies/tui2/vendor/libghostty-vt/src/build/
!.dependencies/tui2/vendor/libghostty-vt/src/build/**
!.dependencies/tui2/vendor/libghostty-vt/src/apprt/gtk/build/
!.dependencies/tui2/vendor/libghostty-vt/src/apprt/gtk/build/**
dist/
out/
artifacts/
release/*.dmg
release/*.zip
release/downloads/*.apk
downloads/*.apk
tmp/imagegen/
.ghostex/
.vsmux/
.antigravitycli/
.playwright-mcp/
.DS_Store
.vscode/
.fresh/
.agents/skills/
.agents/skills/ghostex-release-to-brew/SKILL.md
storybook-static/
apps/desktop/runtime/
apps/editor/macos/.build/
ghostex-paths/target/
ghostex-paths/Cargo.lock
tmp/
.dependencies/ghostty/zig-pkg/
.zig-cache/
# CDXC:PublicRelease 2026-04-27-05:36: Keep generated Storybook output,
# native build products, and runtime logs out of the public repository while
# preserving each developer's local copies.
# CDXC:GhostexTui2 2026-06-16-16:02: The vendored libghostty-vt dependency has
# source directories literally named src/build. Unignore those exact source
# paths so the packaged `gx 2` TUI can build from a fresh clone while ordinary
# generated build/ directories remain ignored.
# CDXC:AgentInstructions 2026-06-13-09:26: Track the repo AGENTS.md so Ghostex
# native layout and hit-testing requirements travel with app source instead of
# living only as a local ignored instruction file. Keep private agent skill
# folders ignored separately.
# CDXC:Distribution 2026-04-28-04:19: Release DMG/ZIP artifacts are uploaded to
# GitHub Releases and referenced by Homebrew casks, so git should track source
# and metadata only instead of committing binary distribution outputs.
# CDXC:Distribution 2026-05-27-10:11: Android APK downloads are also release
# artifacts. Keep local APK copies out of source commits for the same reason as
# macOS DMGs and ZIPs.
# CDXC:Distribution 2026-06-04-01:42: Local Android APK artifacts moved under
# release/downloads so release outputs are grouped together while the old
# downloads/*.apk ignore remains as a guard for stale local scripts.
# CDXC:RepoSafety 2026-07-01-02:45: Ghostty Zig package fetches can materialize under ghostty/zig-pkg during release builds. Keep that generated dependency cache out of release commits while source dependencies stay pinned by build.zig.zon.
# CDXC:RepoSafety 2026-07-06: The standalone GhostexEditor SwiftPM build writes
# compiler outputs under apps/editor/macos/.build. Keep those local build products
# ignored so release commits contain source changes only.
# CDXC:RepoSafety 2026-08-05: The standalone ghostex-paths library writes Cargo
# build output and a library-local lockfile. Keep those generated files out of
# release commits while the workspace lockfile remains the source of truth.
# CDXC:GPUIRuntimeOwnership 2026-07-14: GPUI-owned macOS runtime staging contains
# generated native binaries, bundled Node/code-server payloads, and compiled web
# assets. Keep it local like the retired host's Web staging tree; source inputs
# and the preparation script remain tracked separately.
# CDXC:RepoSafety 2026-05-27-10:44: Image-generation scratch outputs under
# tmp/imagegen are intermediate asset experiments, not source assets. Commit
# only the selected asset destination so releases stay source-only and
# reproducible.
# CDXC:RepoSafety 2026-05-02-00:11: The ghostex release-to-brew skill is local
# agent workflow documentation and must remain available on this machine without
# appearing in the public GitHub repository tree.
# CDXC:RepoSafety 2026-05-10-10:21: Fresh workspace state records local window
# layout for this developer machine and should not be published with ghostex source
# or release metadata.
# CDXC:RepoSafety 2026-05-15-15:03: The pre-rename zmuxHost Web bundle path can
# still receive generated sidebar assets from stale local tooling. Keep it
# ignored like the current ghostexHost Web directory so generated JS/CSS bundles
# never enter release commits.
.idea
react-doctor.config.json
.ignore
.ignored/

# CDXC:ReleaseDocs 2026-05-12-13:09: Local planning notes that should not be
# published with product docs live under `.ignored/` instead of needing
# per-file ignore rules.
.tmp-*

# CDXC:LocalDocs 2026-08-09: Keep the entire docs/ tree on this machine only.
# Planning notes, product drafts, and agent reports are local working material
# and are not part of the public Ghostex source tree.
docs/


# Beads runtime lock files. The CLI creates these while it holds a board or
# embedded-database gate; they are machine state, not board content.
.beads.gate.lock
.beads/*.gate.lock
