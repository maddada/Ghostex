// C1 repo-restructure split: home for main.rs content extracted out of the
// monolithic file. Wave 1 only populates `helpers` (stateless free fns);
// later waves add window/model/god-object modules here too.
pub(crate) mod helpers;
